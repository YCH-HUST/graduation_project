# 第3章 核心辅助诊疗模型设计与训练
本章将详细阐述中医智能辅助诊疗系统中两大核心前置模型——用于舌象视觉特征提取的YOLO目标检测模型，以及用于中医证型与中药方剂推荐的机器学习多分类模型的设计原理与训练过程。通过构建垂直领域的医疗数据集，并实施针对性的特征组合与超参数寻优，本章旨在验证上述模型在辅助诊疗应用场景下的有效性与可靠性。

## 3.1 基于YOLO的舌象目标检测与区域分割
在多模态初诊分析流程中，剥离复杂环境背景并精准定位舌体区域是保证后续形态、色泽识别精度的核心前提。本节重点从网络架构选型、数据增强策略以及训练收敛分析三个维度展开论述。
### 3.1.1 YOLO模型架构选型与优化策略
（此处撰写：为什么选择YOLO某一版本，它在轻量化与推理速度上的优势，以及在该项目中对输入分辨率或损失函数的特定调整）

### 3.1.2 舌象视觉数据集构建与预处理
（此处撰写：原始图片来源，清洗标准，如何利用标注工具打框，以及采用哪些数据增强手段如随机旋转、亮度抖动来提升模型的泛化能力）

### 3.1.3 模型训练过程与性能评估
（此处撰写：训练的软硬件环境配置、Epoch、Batch size、学习率退火策略。最重要的是，此处要**预留图表位置插入您的Loss下降曲线图、mAP评估图**，并用文字描述模型在验证集上的精确率（Precision）、召回率（Recall）及平均精度均值（mAP）。）

## 3.2 基于结构化特征的中医证型与方剂推荐模型
针对传统机器视觉难以捕捉患者症状主因的问题，系统引入了基于门诊量表主诉（如头晕、心悸、脉象等）的结构化病因推理机制，将多维离散特征映射至具体的证型与中药方剂组合。
### 3.2.1 临床诊疗数据的特征工程
（此处撰写：如何将问卷收集到的文字选项转化为机器可读的向量，比如One-Hot编码或词频映射；描述特征矩阵的稀疏性处理手段和数据去伪存真的过滤准则。）

### 3.2.2 机器学习分类算法选型
（此处撰写：简述您所采用的机器学习模型原理，例如是使用了随机森林（Random Forest）、XGBoost、支持向量机还是全连接神经网络。论述该算法在处理中医学非线性、高维特征时的鲁棒性。）

### 3.2.3 模型训练、调优与泛化能力评估
（此处撰写：数据集的划分比例（如8:2的训练集与测试集划分），交叉验证（Cross Validation）策略。**此处需预留图表位置插入模型的混淆矩阵（Confusion Matrix）、各分类标签的F1-Score评估表或ROC曲线**，通过定量数据证明模型在预测证候与药方方向的置信度。）

## 3.3 与大语言模型的串联协同推理
（此处撰写：一小段总结，简述以上两个垂直领域模型（YOLO视觉端与ML结构化文本端）的输出张量或预测标签，最终是如何被统一封装为大语言模型（LLM）的上下文Prompt上下文的，为下一章的系统模块设计做好理论与工程逻辑铺垫。）
